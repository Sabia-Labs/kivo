{{- if .Values.adminApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: kivo-admin-api
  namespace: {{ .Values.adminNamespace }}
  labels:
    {{- include "kivo.labels" . | nindent 4 }}
    {{- include "kivo.adminApi.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.adminApi.service.type }}
  ports:
    - port: {{ .Values.adminApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kivo.adminApi.selectorLabels" . | nindent 4 }}
{{- end }}
