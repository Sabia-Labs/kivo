# ── Global ────────────────────────────────────────────────────────────────────
# These are the base defaults. Override via values-local.yaml / values-uat.yaml / values-prod.yaml

nameOverride: ""
fullnameOverride: ""

namespace: kivo
adminNamespace: kivo-admin

# ── Kivo API (Application Plane) ───────────────────────────────────────────
kivoApi:
  enabled: true
  replicaCount: 1

  image:
    repository: kivo/kivo-api
    tag: "latest"
    pullPolicy: IfNotPresent

  # Port the Express server listens on
  port: 4000

  service:
    type: ClusterIP
    port: 4000

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "500m"
      memory: 512Mi

  # Environment variables injected from a Kubernetes Secret.
  # The secret must be created separately (see charts/kivo/templates/api/SECRETS.md)
  existingSecret: ""

  env:
    NODE_ENV: production
    PORT: "4000"
    # In Kubernetes, ClusterIP Services route traffic via pod IP — not loopback.
    # HOST=0.0.0.0 is required for pod-to-pod communication via the Service.
    # Security boundary: NetworkPolicy (only kivo-web can reach kivo-api) + no external Ingress.
    # The code default (127.0.0.1) applies in bare-metal dev only (outside k8s).
    HOST: "0.0.0.0"
    # DATABASE_URL and JWT_SECRET must come from the secret referenced by existingSecret


  # Run Drizzle migrations as an init container before the API starts.
  # DISABLED: requires --migrate-only flag in apps/kivo-api/src/index.ts (not yet implemented).
  # When implemented, re-enable here and in values-local.yaml.
  runMigrations: false

  # Liveness / readiness probes
  # Use exec (curl on loopback) — not httpGet — because the API binds to 127.0.0.1
  # per the security policy. httpGet probes use the pod IP which cannot reach loopback.
  livenessProbe:
    exec:
      command:
        - sh
        - -c
        - curl -sf http://127.0.0.1:4000/health
    initialDelaySeconds: 15
    periodSeconds: 20
    failureThreshold: 3

  readinessProbe:
    exec:
      command:
        - sh
        - -c
        - curl -sf http://127.0.0.1:4000/health
    initialDelaySeconds: 5
    periodSeconds: 10
    failureThreshold: 3

# ── Admin API ─────────────────────────────────────────────────────────────────
adminApi:
  enabled: true
  replicaCount: 1

  image:
    repository: kivo/admin-api
    tag: "latest"
    pullPolicy: IfNotPresent

  # Port the Express server listens on
  port: 4001

  service:
    type: ClusterIP
    port: 4001

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "500m"
      memory: 512Mi

  # Environment variables injected from a Kubernetes Secret.
  existingSecret: ""

  env:
    NODE_ENV: production
    ADMIN_PORT: "4001"
    HOST: "0.0.0.0"

  runMigrations: false

  livenessProbe:
    exec:
      command:
        - sh
        - -c
        - curl -sf http://127.0.0.1:4001/health
    initialDelaySeconds: 15
    periodSeconds: 20
    failureThreshold: 3

  readinessProbe:
    exec:
      command:
        - sh
        - -c
        - curl -sf http://127.0.0.1:4001/health
    initialDelaySeconds: 5
    periodSeconds: 10
    failureThreshold: 3

# ── Kivo Web (Client Portal) ────────────────────────────────────────────────
kivoWeb:
  enabled: true
  replicaCount: 1

  image:
    repository: kivo/kivo-web
    tag: "latest"
    pullPolicy: IfNotPresent

  port: 3000

  service:
    type: ClusterIP
    port: 3000

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "500m"
      memory: 512Mi

  env:
    NODE_ENV: production
    PORT: "3000"

  livenessProbe:
    httpGet:
      path: /
      port: 3000
    initialDelaySeconds: 20
    periodSeconds: 30

  readinessProbe:
    httpGet:
      path: /
      port: 3000
    initialDelaySeconds: 10
    periodSeconds: 10

# ── Admin Web (Marketing/Admin Portal) ───────────────────────────────────────
adminWeb:
  enabled: true
  replicaCount: 1

  image:
    repository: kivo/admin-web
    tag: "latest"
    pullPolicy: IfNotPresent

  port: 3001

  service:
    type: ClusterIP
    port: 3001

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "500m"
      memory: 512Mi

  env:
    NODE_ENV: production
    PORT: "3001"

  livenessProbe:
    httpGet:
      path: /
      port: 3001
    initialDelaySeconds: 20
    periodSeconds: 30

  readinessProbe:
    httpGet:
      path: /
      port: 3001
    initialDelaySeconds: 10
    periodSeconds: 10

# ── Ingress ───────────────────────────────────────────────────────────────────
ingress:
  enabled: true
  className: nginx
  host: kivo.localhost
  tls:
    enabled: false
    secretName: kivo-tls
    certManager:
      enabled: false
      clusterIssuer: letsencrypt-prod

  annotations: {}

# ── PostgreSQL (embedded — for local/dev only) ────────────────────────────────
postgresql:
  # Set to true to deploy PostgreSQL as a StatefulSet inside the cluster.
  # Set to false in UAT/Prod and provide externalConnectionString via a Secret.
  embedded: true

  image:
    repository: postgres
    tag: "16-alpine"
    pullPolicy: IfNotPresent

  database: kivo
  username: kivo

  # For embedded mode: the password is stored in a generated secret.
  # Do NOT use this in production — use externalConnectionString via a Secret instead.
  password: kivo_local_only

  port: 5432

  persistence:
    enabled: true
    size: 5Gi
    storageClassName: ""
    accessModes:
      - ReadWriteOnce

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "500m"
      memory: 512Mi

# ── Network Policies ──────────────────────────────────────────────────────────
networkPolicy:
  # Enforce strict ingress/egress rules on the API pod.
  # The API is only reachable from the web pod — never from outside the cluster.
  enabled: true

# ── Image Pull Secrets (for ghcr.io or private registries) ───────────────────
imagePullSecrets: []
# - name: ghcr-pull-secret

# ── Agent Controller ──────────────────────────────────────────────────────────
controller:
  enabled: true
  replicaCount: 2

  image:
    repository: ghcr.io/ltreven/kivo-controller
    tag: "latest"
    pullPolicy: IfNotPresent

  # Image the controller injects as the main agent container.
  agentImage: "ghcr.io/ltreven/kivo-agent:latest"
  agentImagePullPolicy: "IfNotPresent"

  # Image the controller injects as the kivo-consumer sidecar.
  consumerImage: "ghcr.io/ltreven/kivo-consumer:latest"
  consumerImagePullPolicy: "IfNotPresent"
