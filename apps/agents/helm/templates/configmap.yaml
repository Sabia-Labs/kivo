apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kivo.fullname" . }}-bootstrap
  namespace: {{ .Release.Namespace }}
data:
  bootstrap.sh: |
{{ .Files.Get "files/bootstrap.sh" | nindent 4 }}
