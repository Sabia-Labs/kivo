apiVersion: v1
kind: Service
metadata:
  name: {{ include "kivo.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "kivo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "kivo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: gateway
      port: {{ .Values.service.port }}
      targetPort: gateway
